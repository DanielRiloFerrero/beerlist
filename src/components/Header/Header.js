import { Container } from "react-bootstrap";

const Header = () => {
  return (
    <header className="bg-dark py-2 mb-3 text-light">
      <Container>Beer explorer!</Container>
    </header>
  );
};

export default Header;
